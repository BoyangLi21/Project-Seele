# eclipse
bin
*.launch
.settings
.metadata
.classpath
.project

# idea
out
*.ipr
*.iws
*.iml
.idea

# gradle
build
.gradle

# other
eclipse
run
run-data
mcmodsrepo
/visual-run.*.log

# Claude Code session data — never publish (local logs/settings)
.claude/

# Local agent instructions/session notes - never publish.
/AGENTS.md
/.Codex/

# Third-party mod jars parked in the repo root for local model extraction
# (Rei Chikita / EUD). All Rights Reserved — never commit or redistribute.
/*.jar
/*.zip
/*.jar.log
/external-assets/
__pycache__/

# Files from Forge MDK
forge*changelog.txt

# Generated evidence: renders, GLB/PDF previews, voxel ledgers, review bundles.
# 1.1 GB and fully reproducible from tools/ - keep it local.
/artifacts/
/backups/
# ProtoMotions/Lightning checkpoints generated by local physics training.
/results/
