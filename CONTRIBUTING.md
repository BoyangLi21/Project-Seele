# 参与 Project SEELE

欢迎从一个明确的小任务开始：一处通道、一组模型接触、一句原创对白、一次可复现的多人操作。项目主要参考 1995 年 TV 版；UN 机体和基地是独立的原创扩展。

## 启动代码

需要 Git、JDK 17；下面的依赖下载助手使用 Python 3.11 或更新版本的标准库。

```powershell
git clone https://github.com/BoyangLi21/Project-Seele.git
cd Project-Seele
python tools/bootstrap_dev_dependencies_r24.py
.\gradlew.bat build
.\gradlew.bat runClient
```

Linux/macOS 使用 `./gradlew`。游戏代码可以按 Forge 的支持平台构建；当前地图修改工具中的 Windows 文件锁尚未全部移植，不能据此宣称整套地图工具已通过跨平台测试。

下载助手获取固定版本的 Moving Elevators、两个公共库与必需的 Minecraft Transit Railway，校验文件后放入忽略目录；Forge 与 GeckoLib 由 Gradle 获取。它不会下载私有地图、模型、账户设置或第三方整合包。**公开代码启动后的回退美术，与开发视频里使用本机资源包的完整世界存在差别。** 当前差异与许可事项见 [公开准备](docs/OPEN_SOURCE_RELEASE_R24.md)。

## 提交前说明什么

- 问题：发生在什么位置、姿态或命令顺序下，实际结果和预期分别是什么。
- 修改：改了哪个责任边界，如何保留原存档、实体 UUID 与已有用户改动。
- 验证：构建结果、实际游戏中的结果、没有验证的场景。截图不能替代穿行、碰撞、联锁或多人权限检查。
- 素材：作者、原始网址、许可、文件校验值、修改说明和可编辑源文件。不要把“能下载”当作“能重新分发”。

地图问题请报告维度、XYZ、站立地面高度、来向／去向，最好附相邻入口和出口。先测真实方块、方块实体和运行中的机械，再提出修改；透视视图、LOD 缓存、剖切效果不能单独作为拆填依据。不要把现有窗户、栏杆、机库液面和移动电梯当作空地。

## 美术和动作接口

- 模型交 `.blend` 或对应可编辑格式、导出的游戏文件、贴图以及骨骼／轴向说明。不要只交渲染图。
- 显示的手、脚、枪口、插入栓口应能和实际约束或命中位置对应。检查站立、跪姿、卧姿、起落与过渡，而不只看一个漂亮的姿势。
- 原 TV、1997 剧场版、Rebuild 与原创设计分开标注。资料页只介绍一本设定集时，不写“已核对全套设定”。
- 街道和房间先说明用途与通行，再做陈设。扶梯保留完整双格、上下端接驳和头部净空；所有高处公共通路保留有效防坠。
- 新对白采用原创措辞；可贡献本人自愿录制的声音及许可，不提交剪自官方作品的配音和 BGM。

## 适合认领的第一批任务

1. 为夏姆榭尔制作可公开分发的原创身体，接入现有鞭骨与接触轨迹；目标是替换本机研究模型。
2. 精修一个站点的灯具、雨棚、窗框或街口，提交实测占地与双向步行结果。
3. 完成一次两名玩家争用同一机体的联机测试，包括断线、取消和恢复。
4. 为一章剧情提交原作顺序、可玩的目标、失败／退出条件与存档恢复方案。
5. 用实际硬件记录一条固定驾驶或航班路线的帧时间，区分服务器 tick、区块传输与客户端渲染。

使用 issue 先写清范围，避免多位贡献者同时覆盖同一存档。代码采用 MIT；外部或衍生美术继续遵守各自的许可。计划、草图、开发测试、人工验收通过的效果要明确区分。
