# Project SEELE 地图微改短流程

本规范合并 Claude 2026-08-08 交接中的实测加速建议与 GPT-5.6 Pro 的空间孪生方案。
它用于已有存档的细微修复；不是自动生成新设施的授权。

> 2026-09-07 当前工程说明：用户已明确授权继续修复、扩建和精修实际游玩的
> `SEELE_TV_WORLD_PREVIEW_20260906`，并要求在其离家期间自主完成全图检查。
> 本次授权范围采用 R02/R03 的冷备份、逐格差量和原生通行验证，可批量处理已规划设施，
> 不再逐地点等待人工审批。当前文件和 [R03 记录](WORLD_QUALITY_R03.md) 是接续依据；
> R28 保留为历史源档，不能用它覆盖当前预览。下文地点卡和逐次裁决流程适用于尚未明确
> 获得工程授权的局部改动。

> 2026-09-08 R04 已续建并完成实测，当前金字塔、Terminal Dogma 与武器动作以 [R04 记录](WORLD_MOTION_R04.md) 为准。旧 Dogma 内侧台面已退休，不能根据 R03 的旧路线重填 LCL 上方空间。新房间的转换梁不得重新向下穿过既有走廊。

> R05 的动作修复与三张现存结构斜视图见 [R05 记录](EVA_MOTION_R05.md)。斜视图局部剖开屋顶与近墙，不能据此判定存档存在破洞；当前 TV 存档本轮未写入方块。

> R06 已确认并修补实际围护缺口，当前状态见 [R06 记录](WORLD_MOTION_R06.md)。机场西联络通道通过新梯段从 Y71 接到 Y65 站台；保留新楼梯与墙边座椅，不能再用旧直通高层桥面覆盖。港口图是测绘后的准备方案，尚未建造。完整实走目录为 8,338 项，其中历史结果与本轮局部复验需按来源区分，不能把复跑数当新增覆盖。

> 2026-09-09 R07：负责人已授权并实际开建湾岸港区、远郊军事基地和地表试验机库。港口现已施工，不再按 R06 的空地准备方案处理。新增 182 条原生步行检查全部通过，完整路线目录为 8,520 条；旧区沿用既有结果，不冒称本轮重跑全图。P1 的十四个新 MTR 对象以追加方式安装；后续只更新其四个排班/停车对象。不得重置旧交通数据库。地表 LCL 舱与试验机 UUID 由独立 `projectseele_military_r07` SavedData 管理，不得用原三机编制替代。当前设施坐标与后续验收状态见 [R07 记录](WORLD_EXPANSION_R07.md)。

## 三条绝对规则

> 2026-09-11 R16：机库和井内饰面改为 `nerv_machine_panel`、`nerv_shaft_panel` 等灰绿／蓝灰材料；九道封口位于 Y=-332、-192、-52。三井 31×31 核心和栈桥必须保持净空，运行维护不得恢复紫白旧模板。地表门板改为分段收纳，外壳不超过井中心左右 20.7 格；这是运行时机械模型，不能根据打开后的截图向井口填方块。新增冬月后工作人员为 229 个岗位，原 228 个岗位身份保留。见 [R16](TV_FACILITIES_R16.md)。

> 2026-09-11 R15：当前人员名单 `nerv_staff_r15.json` 为 228 个岗位，和 `regional_plan.json`、各维度 SavedData／实体数据一同迁移。指挥室电梯原有人工轿厢、按钮和显示屏 NBT 已恢复；地板改为新饰面时必须兼容轿厢识别，不得因颜色变化重建或搬回最低层。Distant Horizons 的条带、缺面可能是未完整生成的缓存；本轮在正确生成配置和较高垂直细节下已验证地上与地下连续远景，不能据不完整 LOD 去填真实地图。完整 8,550 条通行目录保留，本轮复验其中 201 条。见 [R15](STAFF_WORLD_R15.md)。

> 2026-09-10 R14：原 291,258 块强化深板岩已改为 `nerv_structural_panel`，维护识别保留旧材料兼容；不得用旧模板补回粗糙饰面。屋顶接待房间、东侧三级梯段和钢琴新位置已回读、实走，完整目录为 8,550 项。顶部会客厅应从 X=10/11、Z=311 的可开合门板进入，不能把 X=15 的黑色展示墙当成通道。大指挥室内部布局、2,344 块单向窗和原机体身份保留。见 [R14](WORLD_REFINEMENT_R14.md)。

> 2026-09-10 R11：本轮已重跑全部 8,538 条原生通行目录，扫描并清理 8 个确认悬空的自然碎片，26 块军事标牌改为 UN。基地、舰体和载具徽记属于模型／渲染资源，不据其截图覆盖地图。原 3＋31＋84 个受管身份保留；新增 UN 插入栓 `c331d78c-a22b-4892-afee-35390ee0df35` 属于独立 EVA-UN，不得作为远离 NERV 机库的旧插入栓清除。试验舱仍为注液关闭。见 [R11](WORLD_MOTION_R11.md)。

> 2026-09-09 R10：34 间新房间细化、工作椅与连续战术屏、四片植被区和东北迎击大道已经实装。实际大道为 X=316..396 / Y=80 / Z=-512..-278，出动终端在 84/-433/274。新的灯柱位于人行道外缘，不能按最初计划放回步行线。164 条相关路线通过原生实走，完整目录恢复到 8,538 条。金字塔外壳、2,344 块单向窗及原受管实体保留；见 [R10 记录](WORLD_MOTION_R10.md)。

> 2026-09-09 R08：港口原两艘简化舰体及其 24 条旧舰路线已退役，现为私有导入的两艘晓级舰，登舰点在 Z=420。完整路线目录为 8,528 项，原受管 31 个身份保留，84 个细部构件由独立 R08 SavedData 管理。金字塔东侧 Y=-449..-445 的旧土石已改为支撑结构，Y=-444 步行面保留。原发射井之间 X=7..11、49..53 / Z=207..237 的漏迁移隔墙已清理，不能再按“缺墙”补回；实际发射井在 Z=-53..-19。三个发射井黑色外罩及机库屋面只改变外侧，内部保留证明及最新完整／剖切图见 [R08 记录](WORLD_REFINEMENT_R08.md)。

> 2026-09-09 R09：根据负责人新照片明确退役东侧长平台本体，R08 的保留平台／梁柱记录至此失效，不能按旧记录重建。保留实际人员电梯、机库连廊与科研桥接驳。金字塔轮廓不变，外壳统一深色并封闭失效高位南入口。顶部窗现为 `one_way_glass[pyramid=true]`，必须保留对应方块实体 NBT。Dogma 的旧 X=66..79/Z=266..281 预留区不是在用电梯，已退役；真正电梯仍是 X=9/Z=253。358 项相关路线实走通过，完整目录仍为 8,528 项。以 [R09 记录](WORLD_REFINEMENT_R09.md) 和当前存档为准。

1. `git log` 和用户指定的当前存档是基线；本次工程使用
   `SEELE_TV_WORLD_PREVIEW_20260906`。
2. `tools/query_blocks.py` 是唯一的存档方块读取入口；不得再复制区块、调色板和
   Anvil 解码代码。
3. 每一个拟写坐标都必须能回答“实测旧状态是什么、为什么允许改、怎么还原”。

空气、可站立面、开放组件、碰撞检测通过，都不等于房间、通道或建设许可。未知区域
一律 `HOLD`。

## 地点卡

一轮只处理一个地点卡：

```json
{
  "id": "PLACE_ID",
  "world": "SEELE_S20_RECOVERY_R28",
  "anchor": [0, 0, 0],
  "facing": "north|south|east|west",
  "purpose": "人类确认的用途",
  "in": {"pos": [0, 0, 0], "normal": [0, 0, 0]},
  "out": {"pos": [0, 0, 0], "normal": [0, 0, 0]},
  "neverTouch": [],
  "mustShow": [],
  "mustNotShow": [],
  "reference": [],
  "acceptanceCamera": []
}
```

地点、朝向、用途、入口或出口任一不确定时，停止并输出证据，不得猜。

## 5–10 分钟局部闭环

1. **0–1 分钟：地点卡。** 把真人自然语言、F3 坐标、面向和截图落成卡片。
2. **1–2 分钟：只读实测。** 用 `query_blocks.py` 读局部 box；先画 ASCII 平面或剖面。
3. **2–4 分钟：空间证据。** 仅在 ASCII 仍不足时，调用
   `survey_facility_target.py` 生成入口/出口内外双向视图、中心线双向视图、横纵剖面、
   45° 视图和局部 GLB。图上必须有坐标、北箭头、Y 标尺、端口法向和材质图例。
4. **4–6 分钟：唯一 ghost。** 只生成一个候选，不并行生成多个世界让真人挑。
5. **6–7 分钟：可逆差异。** 导出精确 `positiveEditMask`、逐格旧状态前置条件、
   `forwardPatch` 和 `inversePatch`。
6. **7–9 分钟：视觉异议。** Claude/视觉模型只指出走向、遮挡、比例或原作约束冲突；
   不写世界、不改事实源。
7. **9–10 分钟：真人裁决。** 批准前只写一次预览副本；批准后才另行写权威存档。

## 通道判定

从真人指定的通道种子开始，只做局部分析：

- 脚下必须有连续承重面，脚位和头部净空连续；
- 局部 flood-fill 后骨架化为中心线，每两格采样一次断面；
- 记录净宽、净高、地板、墙、窗、栏杆、支撑和材料；
- 门平面、楼梯首级、电梯层门、岔路或已命名地点才允许成为端口；
- 裁剪边缘只能标 `UNKNOWN`，不能当出口；
- 窗和栏杆不能当入口；装饰楼梯不能当楼层连接；
- 电梯必须有连续井道和两侧层门；楼梯必须连接不同 Y 层；
- 新段默认继承相邻通道中心线、净宽和净高。

端点未知、法向未知、无支撑、触及 `HARD/HOLD`、或在扫描边缘终止时立即
`NO-BUILD`。

## 写入协议

每格写入必须同时满足：

```text
坐标属于 positiveEditMask
当前完整方块状态/NBT 等于预览时记录的旧状态
坐标不属于 HARD/HOLD
补丁内存在该格的 inversePatch
```

人工修改登记为 `owner=human`。如果当前状态已不在实测允许集合里，说明真人改过，
运行时生成器必须跳过；世界本身就是记录。

每次只刷新补丁 bbox 外扩两格内的局部语义图。禁止每轮重算全世界哈希、全图连通、
全局支撑或自动寻找“可建房间”。只有基线建立或明确诊断时才运行全局工具。

## 工具与模型分工

- `query_blocks.py`：所有局部方块实测和 ASCII 图；首选。
- `survey_facility_target.py`：只在需要视觉空间证据时生成局部证据板和 GLB。
- `facility_map.py` / `facility_audit.py`：只产出候选证据，绝不赋予语义或写入许可。
- GPT-5.6 Pro：整理原作约束和地点语义，解决语义冲突。
- Codex：确定性读取、证据生成、局部 diff、可逆补丁和批准后的执行。
- Claude/视觉模型：独立看证据，提出异议；不修改世界或地点身份。
- 真人：唯一的地点命名、端口确认、审美批准和权威存档写入授权者。

禁止并行决定地点身份、端口连接和世界写入。单一事实源应是
`spatial_twin.json` 与 patch journal，而不是无限增长的工作日志。

## 开销纪律

- 不再逐轮读取或追加 `.Codex/ACTIVE_WORKLOG.md`；历史只在考古时读。
- 活跃约束放在本文件、相关源码注释或不超过 200 行的短决策清单里。
- 不为一次查询新建数百行脚本；优先使用 `query_blocks.py` 或十几行内联调用。
- 不靠试错寻找已经存在于源码、NBT、字节码或真人作品里的参数。
- 分不清时先输出 ASCII 剖面或证据板，停止写入。
- 只在真人批准前拷一次预览副本；写冻结存档前导出局部还原快照。
