# R27 变更记录

- 冬月的岗位从 `24 -389 349` 移至最高指挥台 `27 -406 277`，原司令椅在 `28 -406 277`，城市原生按钮在 `26/30 -405 277`。只修改原 NPC 的位置、朝向、归位岗位及名册，不修改任何方块、不创建替代 NPC。
- 电话新增冬月专属城市页，城市升降通过角色权限检查、NPC 寻路、按键动画和 `NervOperationsConsole` 的原生调度完成。带疑问或否定的文字不会执行升降。
- 机库机械锁定及转移期间，F5 转轴从原先最终 30 米修正为胸肩 48 米；原有碰撞和沿转移路线的前瞻缩放保留。
- 没有改变伤害、血量、城市升降速度、外部资源或网络数据格式。协议仍为 32。

复测依据与三包校验值存于本机 `artifacts/facility_r27`、`artifacts/server-ready-r27`。地图、私有模型和打包产物不提交 Git。

原生副本验证：冬月两次实体按钮操作成功，城市实际下沉 6 米后升回地表；带疑问／否定的输入及警卫越权检查通过。有人驾驶的完整整备、发射、回收循环记录 308 个服务器采样；前后 F5 共 2,032 帧，实测镜头中心相对脚位在 47.998–48.004 米之间，并核看机库、井内截图。正式存档仅移动冬月，另 648 个原有实体 NBT 保持一致。

打包后的正式 Forge 47.4.10 专用服务器在隔离副本启动成功，确认新岗位冬月与地表电源柱可加载，并正常保存退出；这不是远程玩家登录测试。三包含同一 SHA-256 的私有模组 JAR。
