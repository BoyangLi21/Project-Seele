# R31 联合国双机库扩宽

基线为用户实际游玩的 `SEELE_R30_WORLD`，这次只施工到 `SEELE_FIELD_R31_REVIEW`。没有直接写正式源存档。

| 几何 | 原尺寸 | R31 尺寸 |
| --- | --- | --- |
| 每库外壳宽度 | 117 格 | 133 格 |
| 每座湿舱／机体通道净宽 | 33 格 | 49 格 |
| 机体大门净宽 | 33 格 | 49 格 |
| 大门净高 | 65 格 | 65 格 |
| 每座可注液区 | 130,680 格 | 194,040 格 |

机库原点不变：UN-00 为 `6442.5,77,-6205.5`；UN-01 为 `6282.5,77,-6205.5`。仍朝 +Z 出库。两座机库之间的服务道路、控制室、原登机楼梯、插入栓登机开口和角色岗位保留。

实测当前私有模型静态网格宽度为 24.375 格和 31.3125 格。扩宽后，居中通行时的单侧余量分别约 12.31 和 8.84 格。这是网格静态外包络的测量，不是假称所有战斗姿态已经实机通过。原 33 格门对 UN-01 每侧只剩约 0.84 格。

维护栈桥向两侧各移 8 格，重新接回原登机点。控制室东侧旧人员通道会被扩大的湿舱占用，因此从原人员入口改走控制室西侧，再从后方接回原楼梯；通行目录已同步。UN-01 旧门前一条横挡人员入口的低栏也已开口。

机体门改为 UN 专用的四段顶部叠收结构，收纳在 Y142–160 的封闭门箱。直接把旧侧移门拉宽会扫到人员入口，所以没有沿用那个扫掠范围。门片各处于独立导轨深度，收拢时不互相穿插；UN 标识按门片分段随动。原 NERV 三机库门的外形和开合方式不变。

## 运行时接入

- `un_hangars_r31.json` 是迁移后的世界标记。只有带此标记的世界采用新尺寸；旧存档继续使用原 33 格湿舱和侧移门。
- `UNHangarDimensionsR31` 统一提供湿舱、门封口和扫掠范围。`MilitaryR07Director`、`UNAnnexR20` 的注排液迭代已用相同宽度，不会只清掉中间 33 格。
- SavedData 增加 `PoolWidth`。如果在读入迁移世界时遇到进行中的旧宽度注排液，会从第一层重新遍历；原机体 UUID、栓、阶段和在库／出库状态均不重置。
- 原存档 UN-00 为 WET、UN-01 为 OPEN；本次静态施工保持这两个状态。
- 大门宽度通过实体同步数据传给客户端，UN 警示灯位置相应移至新门箱。

## 凭据与限制

`tools/widen_un_hangars_r31.py` 通过 `scan_regional_completion`／`query_blocks` 读取实际块状态，以 `regional_voxels.Painter.match` 写逐格差量。已产生 259,026 格修改，126 个拟涉及区块，2 个 region；精确修改区块数以 receipt 为准。

主收据：`artifacts/facility_r31/un_hangars/widening/applied_20260923_094331_755698/receipt.json`。同目录 `delta/` 含前后状态与反向恢复所需数据，`before/` 是对应 region 冷备份。`installed.json` 记录世界标记和通行目录前后散列。原控制元数据和 14 个方块实体未改。

施工前已检查湿舱底与侧壁、整幅屋顶、两库共 946 个水平通行样本；Painter 施工后逐格回读与计划一致。新增 12 项原生路线定义在 `walk_cases.json`，含原人员楼梯完整登机来回路线。这里没有启动 Minecraft，动态开门、排液、开机进出仍交给主任务统一构建及验收。
